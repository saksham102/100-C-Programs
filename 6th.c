/* Product of two no. */
# include <stdio.h>
int main(){
    int a, b, product;

    printf("aenter two no. : ");
    scanf("%d %d",&a,&b);

    product = a*b;

    printf("product = %d", product);
    return 0;
}
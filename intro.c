/* Print Sum of Two No. */
#include <stdio.h>

int main(){
    int a,b,sum;

    printf("Enter Two numbers : ");
    scanf("%d %d", &a, &b);

    sum = a + b;

    printf("Sum = %d",sum);

    return 0;
}